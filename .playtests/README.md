# Playtest reports

Hands-on playtest write-ups, one file per session, named `YYYY-MM-DD-patch-N.NN.md`.

These are reference material only. Nothing in the game or `app/` loads or reads this folder,
and no doc points here. **Agents: do not read the reports in this folder unless the user asks
for them** - they are past impressions, not current rules.
